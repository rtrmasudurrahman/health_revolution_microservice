// import { OrderSchema, CartItemSchema } from "@/schemas";
// import { z } from "zod";

// export const validateOrder = (order: any) => {
//   return OrderSchema.safeParse(order);
// };

// export const validateCartItems = (items: any) => {
//   return z.array(CartItemSchema).safeParse(items);
// };
